function [filename,AcquisitionDate,biologicalreplicate,cell1,pili,cellposition,Hops,framestart,framestop,piluslength] = importfileChpData2(workbookFile,sheetName,startRow,endRow)
%IMPORTFILE Import data from a spreadsheet
%   [filename,AcquisitionDate,biologicalreplicate,cell1,pili,cellposition,Hops,framestart,framestop,piluslength]
%   = IMPORTFILE(FILE) reads data from the first worksheet in the Microsoft
%   Excel spreadsheet file named FILE and returns the data as column
%   vectors.
%
%   [filename,AcquisitionDate,biologicalreplicate,cell1,pili,cellposition,Hops,framestart,framestop,piluslength]
%   = IMPORTFILE(FILE,SHEET) reads from the specified worksheet.
%
%   [filename,AcquisitionDate,biologicalreplicate,cell1,pili,cellposition,Hops,framestart,framestop,piluslength]
%   = IMPORTFILE(FILE,SHEET,STARTROW,ENDROW) reads from the specified
%   worksheet for the specified row interval(s). Specify STARTROW and
%   ENDROW as a pair of scalars or vectors of matching size for
%   dis-contiguous row intervals. To read to the end of the file specify an
%   ENDROW of inf.%
% Example:
%   [filename,AcquisitionDate,biologicalreplicate,cell1,pili,cellposition,Hops,framestart,framestop,piluslength] = importfile('Chp-cAMP_experiments_Lin_2.xlsx','fliC-',2,715);
%
%   See also XLSREAD.

% Auto-generated by MATLAB on 2020/05/21 15:12:29

%% Input handling

% If no sheet is specified, read first sheet
if nargin == 1 || isempty(sheetName)
    sheetName = 1;
end

% If row start and end points are not specified, define defaults
if nargin <= 3
    startRow = 2;
    endRow = 715;
end

%% Import the data
[~, ~, raw] = xlsread(workbookFile, sheetName, sprintf('A%d:J%d',startRow(1),endRow(1)));
for block=2:length(startRow)
    [~, ~, tmpRawBlock] = xlsread(workbookFile, sheetName, sprintf('A%d:J%d',startRow(block),endRow(block)));
    raw = [raw;tmpRawBlock]; %#ok<AGROW>
end
raw(cellfun(@(x) ~isempty(x) && isnumeric(x) && isnan(x),raw)) = {''};
cellVectors = raw(:,[1,2,6,7]);
raw = raw(:,[3,4,5,8,9,10]);

%% Create output variable
I = cellfun(@(x) ischar(x), raw);
raw(I) = {NaN};
data = reshape([raw{:}],size(raw));

%% Allocate imported array to column variable names
filename = cellVectors(:,1);
AcquisitionDate = cellVectors(:,2);
biologicalreplicate = data(:,1);
cell1 = data(:,2);
pili = data(:,3);
cellposition = cellVectors(:,3);
Hops = cellVectors(:,4);
framestart = data(:,4);
framestop = data(:,5);
piluslength = data(:,6);

